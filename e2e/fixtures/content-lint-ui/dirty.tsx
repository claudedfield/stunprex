// D-WEB-23 fixture: em-dashes inside user-visible strings. The lint must fail here.
// A comment may say anything, including an em-dash — which the lint ignores.
export const title = 'StunpreX — Train Smarter';
export const pair = ['https://stunprex.com', 'Home — Start'];
